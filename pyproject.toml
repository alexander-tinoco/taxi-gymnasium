[project]
name = "taxi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "alexander-tinoco", email = "alexander.tinoco.gh@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "gymnasium>=1.3.0",
    "pygame>=2.6.1",
]

[project.scripts]
taxi = "taxi:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
